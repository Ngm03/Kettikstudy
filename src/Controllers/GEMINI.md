# КОНТЕКСТ: КОНТРОЛЛЕРЫ

Каждый контроллер в этой директории должен возвращать строгие HTTP-статусы. 
При обработке POST-запросов (например, форма заявки для поступления в Польшу), контроллер обязан валидировать данные перед отправкой в модель. При успешной заявке возвращать JSON для фронтенда.
