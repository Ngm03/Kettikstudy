[СИСТЕМНЫЙ ЖУРНАЛ]
2026-05-23: Проект инициализирован. Базовая структура директорий создана. Ожидание Фазы 1.
-- Это была устаревшая запись. Реальное состояние ниже. --

2026-05-23 (Аудит + Рефакторинг):
ВЫПОЛНЕНО: Блок 1 (Security Hotfixes)
  - Удалены SQL-дампы (1.4 МБ данных реальных пользователей) из репозитория
  - AuthService.php: убран JWT fallback на md5(__DIR__). Теперь падает 500 без JWT_SECRET.
  - .gitignore: добавлен явный запрет на *.sql и /migrations/*.sql

ВЫПОЛНЕНО: Блок 2 (Актуализация контекста)
  - BLUEPRINT.md полностью переписан — отражает реальную архитектуру (роли, таблицы, сервисы)
  - .todo.json заменён — задачи техдолга вместо фантомных "Фаза 1"

ВЫПОЛНЕНО: Блок 3 (Middleware)
  - Созданы src/Middleware/AuthMiddleware.php и RoleMiddleware.php
  - Router.php обновлён — поддержка middleware-цепочек
  - index.php обновлён — все /admin/* и /manager/* маршруты защищены через Middleware
  - AdminController.php: удалены checkAdmin() и checkAdminOrManager()

ВЫПОЛНЕНО: Блок 4 (Расщепление CommunityChatController)
  - Создан src/Services/Chat/ChatRoomService.php
  - Создан src/Services/Chat/MessageService.php
  - Создан src/Services/Chat/FileAttachmentService.php (MIME через finfo)
  - Создан src/Services/Chat/AiBotService.php
  - CommunityChatController.php переписан (~150 строк, делегирует сервисам)

СЛЕДУЮЩИЙ ПРИОРИТЕТ: Отчет по аудиту бизнес-логики (P0-P3).

2026-05-23 (Бизнес-логика, P0):
ВЫПОЛНЕНО: Шаг 1 (Заморозка мусора)
  - В dashboard.php студента скрыты ссылки на /dashboard/prices. Чат переименован в "Чат с менеджером".
ВЫПОЛНЕНО: Шаг 2 (Финансовая воронка)
  - Подготовлен SQL-патч (sql_patch.sql) для продакшена (study_payment_receipts, receipt_id).
  - Создан PaymentController.php (загрузка, просмотр, approve/reject).
  - Настроены роуты в public/index.php.
  - UI Студента: добавлен блок загрузки чека в dashboard/home.php.
  - UI Менеджера: добавлена таблица Action Queue (Ожидают подтверждения чека) в manager_dashboard.php.
  - При одобрении чека статус студента автоматически меняется на enrolled.

СЛЕДУЮЩИЙ ШАГ: Безопасность документов (MIME) и восстановление пароля.

2026-05-23 (Безопасность и Восстановление доступа, P0):
ВЫПОЛНЕНО: Шаг 3
  - DocumentController::upload(): внедрена проверка MIME через finfo_file.
  - DocumentController::upload(): реализована логика UPDATE (замена старого файла) вместо пложения дублей для паспортов и аттестатов.
  - Создан PasswordResetController для генерации токенов и сброса паролей.
  - login.php: добавлен UI и JS логика переключения между формами входа, запроса ссылки и установки нового пароля.
  - MVP-костыль: токен выводится прямо на экран (красным текстом) при запросе восстановления, чтобы можно было кликнуть без реального письма.
  - Таблица study_password_resets уже была добавлена в sql_patch.sql на предыдущем шаге.

2026-05-23 (Action Queue для менеджера):
ВЫПОЛНЕНО:
  - Создан эндпоинт /api/manager/action-queue (слияние ожидающих чеков и документов, сортировка по дате).
  - Дашборд менеджера обновлен: таблица "Ожидают подтверждения чека" заменена на "Единая очередь задач (Action Queue)".
  - Добавлена логика одобрения/отклонения документов прямо из дашборда менеджера.

2026-05-23 (Архитектура ролей и RBAC):
ВЫПОЛНЕНО: Шаг 4
  - Усилена защита в AdminController: добавлены методы canAccessStudent и canAccessDocument.
  - Менеджеры теперь физически не могут просматривать, редактировать или одобрять документы чужих студентов.
  - Менеджерам запрещено переназначать студентов на других менеджеров (отключено обновление manager_id для роли manager).
  - Эндпоинт getAllDocuments теперь возвращает только документы привязанных студентов, если запрос делает менеджер.
  - RoleMiddleware уже был внедрен ранее и корректно защищает маршруты в public/index.php.

СЛЕДУЮЩИЙ ПРИОРИТЕТ: Шаг 5 (Рефакторинг UI/UX - P2) по указанию бизнеса.

2026-05-24 (Визуальные и логические баги):
ВЫПОЛНЕНО: Bug Fix Session
  - views/home.php: Восстановлена Hero-секция. Исправлена сломанная структура HTML
    (блок статистики + закрывающие теги от удалённой старой dark-hero секции висели
    снаружи </section>). Статистика вынесена в отдельную section с тёмным градиентом
    (from-slate-900 via-blue-950 to-indigo-950), карточки получили корректные Tailwind-классы.
    Изображение в hero восстановлено с корректным alt-атрибутом и loading="eager".
  - views/layouts/dashboard.php: Исправлен порядок загрузки Tailwind CSS —
    перемещён в начало <head> перед кастомными CSS-файлами, чтобы preflight
    не перезаписывал правила dashboard.css.
  - public/assets/css/dashboard.css: Усилены ограничения размеров SVG-иконок в навигации
    (min-width, max-width, flex-shrink: 0) для предотвращения растяжения.
  - src/Controllers/AuthController.php: Все хардкодные английские тексты ошибок
    (Invalid credentials, Email and password are required, Invalid email format,
    Password must be at least 6 characters, User already exists, Registration failed)
    обёрнуты в __() для полной локализации.
  - lang/ru.json: Добавлены 7 новых ключей локализации (invalid_credentials,
    error_email_password_required, error_invalid_email, error_password_min_length,
    error_user_exists, register_success, error_registration_failed).
  - lang/kk.json: То же самое на казахском языке.
  - Все файлы прошли php -l и json_decode валидацию без ошибок.
  - Бэкапы: home.php.bak2, dashboard.php.bak, AuthController.php.bak, ru.json.bak, kk.json.bak.

2026-05-24 (Критический баг P0 — Регистрация):
ВЫПОЛНЕНО:
  - Реакция на критический инцидент (P0): Исправлен тихий сбой при регистрации пользователей.
  - src/Controllers/AuthController.php: Внедрено явное управление транзакциями через `PDO::beginTransaction()`, `PDO::commit()` и `PDO::rollBack()`.
  - Обе вставки (`INSERT INTO study_users` и `INSERT INTO study_leads`) объединены в атомарную транзакцию. Любой сбой в одной из них приводит к полному откату изменений, предотвращая появление "битых" записей или ложного успеха при сбое базы данных.
  - Безопасная обработка ошибок: Перехваченные исключения `PDOException` и обычные `Exception` теперь безопасно логируются на стороне сервера (`error_log`) с полным стек-трейсом для внутреннего дебага.
  - На сторону клиента (фронтенд) возвращается только безопасный локализованный текст `__('error_registration_failed')` без раскрытия внутренней структуры базы данных и деталей таблиц/колонок.
  - Изменения успешно валидированы с помощью `php -l` на синтаксическую корректность.

2026-05-24 (Session Hydration и устранение 403 ошибок):
ВЫПОЛНЕНО:
  - src/Middleware/AuthMiddleware.php: Реализована Session Hydration (синхронизация глобального состояния). В конструктор добавлена инициализация `$this->db` через `Database::getInstance()->getConnection()`. В методе `handle()` на каждом HTTP-запросе производится легковесный `SELECT role FROM study_users WHERE id = ?`. Свежая роль записывается в JWT `$decoded['role']` и принудительно синхронизируется с `$_SESSION['user_id']`, `$_SESSION['user']['id']` и `$_SESSION['user']['role']`.
  - src/Middleware/RoleMiddleware.php: Устранена уязвимость регистронезависимости и пробелов в проверке ролей. Все сверяемые роли нормализуются через `strtolower(trim())` как для декодированного токена, так и для массива `$this->allowedRoles`.
  - Валидация: Оба файла Middleware успешно верифицированы с помощью `php -l` на отсутствие синтаксических ошибок.

2026-05-24 (Спринт 1: Очистка интерфейса и Реальная Аналитика):
ВЫПОЛНЕНО:
  - Очистка интерфейса: Полностью удален раздел «Сообщество» (Community). Удалены файлы `src/Controllers/CommunityController.php` и `views/admin/community.php`, вырезаны соответствующие GET и API роуты из `public/index.php`, убран метод `communityPage` в `AdminController.php`, выпилен пункт меню из сайдбара и заголовков в `views/layouts/admin.php`.
  - Бизнес-Аналитика: Полностью оживлена страница аналитики (`views/admin/analytics.php`) с переходом на реальные данные из БД.
  - Разработаны и внедрены эффективные SQL-запросы в `AnalyticsController::getDashboardStats()`: общее число студентов (`totalStudents`), новые лиды за последние 30 дней, горячие лиды, конверсия воронки (отношение `enrolled` к общему объему лидов), распределение воронки по статусам, нагрузка по менеджерам, топ направлений обучения и список последних зарегистрированных студентов.
  - Фронтенд: Переписаны графики в `public/assets/js/analytics.js` (круговая диаграмма для воронки статусов и горизонтальный столбчатый график для визуализации нагрузки менеджеров) и таблица последних студентов с отображением ассистентов и дат.
  - Все затронутые PHP файлы успешно прошли проверку `php -l`.

2026-05-24 (Спринт 2: Зачистка фронтенд-ошибок, оживление настроек и логов):
ВЫПОЛНЕНО:
  - Зачистка фронтенда и отключение Web Push:
    - Полностью удален файл `views/admin/push-subscribe.php`.
    - Убраны кнопка включения push-уведомлений, соответствующий JS-скрипт и подключение файла `push-subscribe.php` из глобального макета `views/layouts/admin.php`.
    - Из `views/admin/prices.php` и `views/dashboard/prices.php` полностью вырезаны JS-fetch запросы к `/api/community/prices` и `/api/community/prices/delete`. Элементы интерфейса отчетов "Сообщества" скрыты, выводятся чистые и безопасные офлайн-сообщения.
  - Раздел "Логи":
    - Перехвачен стандартный системный `error_log` прямо на старте жизненного цикла приложения в `public/index.php`. Все системные логи теперь направляются в локальный для проекта файл `logs/app.log`, находящийся внутри рабочего пространства.
    - В `AdminController.php` переписан метод `getLogs()`, считывающий последние 150 строк `logs/app.log` и парсящий их на уровни серьезности: `error` (Fatal/Error/Exception), `warning` (Warning/Notice) и `info` (Info).
    - Внедрен безопасный метод `clearLogs()` с роутом `POST /api/admin/logs/clear`, который осуществляет быструю очистку (усечение) логов и записывает это событие в административный журнал базы данных.
    - Полностью обновлен шаблон `views/admin/logs.php` с использованием премиального Tailwind CSS: добавлены счетчики-виджеты ошибок, динамическая цветовая подсветка строк лога и кнопка "Очистить логи".
  - Раздел "Настройки":
    - Полностью переписан интерфейс настроек в `views/admin/settings.php`. Внедрен лаконичный и удобный список глобальных параметров продукта: "Контактный email платформы" (`platform_email`), "Режим технических работ" (`maintenance_mode`) и "URL для пользовательского соглашения" (`terms_url`).
    - Адаптирован скрипт автосохранения (debounced save): корректно считывается и сохраняется статус чекбокса (toggle switch) для режима техработ.
    - Подтверждена интеграция эндпоинтов `getSettings()` и `updateSettings()` в `AdminController.php`.
  - Синтаксическая валидация:
    - Проведен запуск `php -l` на всех измененных PHP файлах. Синтаксических ошибок не обнаружено.

2026-05-24 (Спринт 3: UI/UX Полировка и Локализация):
ВЫПОЛНЕНО:
  - Полное удаление "Рассылок" (Newsletters/Push):
    - Полностью удален файл `views/admin/notifications.php`.
    - Из глобального макета `views/layouts/admin.php` вырезана ссылка на `/admin/notifications` и ключ `'notifications'` из заголовков.
    - Из `public/index.php` полностью удалены роуты для рассылок: `GET /admin/notifications`, `POST /api/admin/broadcast`, `GET /api/admin/broadcast-history`.
    - Из `AdminController.php` удалены неиспользуемые методы `notificationsPage()`, `sendBroadcast()` и `getBroadcastHistory()`.
  - Локализация Аналитики:
    - Все хардкодные русские строки в шаблоне `views/admin/analytics.php` обернуты в функцию перевода `__('...')`.
    - Внедрен глобальный Javascript-объект `window.I18N_ANALYTICS` в `views/admin/analytics.php`, через который локализованные лейблы передаются из PHP напрямую в клиентский JS.
    - Обновлен файл `public/assets/js/analytics.js`: статус-лейблы воронки, подписи на графиках Chart.js, динамические бейджи ("vs пред. 30 дней", "Без изменений") и текстовые заглушки в таблицах теперь считываются из `window.I18N_ANALYTICS` и полностью поддерживают русский и казахский языки.
    - Добавлены новые ключи локализации (для статусов лидов, динамики и графиков) в языковые словари `lang/ru.json` и `lang/kk.json`.
  - Починка Модальных окон (ВУЗы и Менеджеры):
    - В `views/admin/universities.php` скорректированы стили класса `.au-modal-overlay` (добавлен `z-index: 9999 !important;`), что полностью исключило перекрытие модального окна боковым меню или шапкой и обеспечило идеальное центрирование в окне просмотра.
    - В `views/admin/managers.php` аналогично скорректирован `.am-modal-overlay` (`z-index: 9999 !important;`) для ровного и надежного отображения.
    - Решен баг с выводом сырого ключа `global_select_user`: в языковые файлы `lang/ru.json` и `lang/kk.json` добавлен перевод для `"global_select_user"`. В `views/admin/managers.php` вызовы перевода заменены на отказоустойчивые конструкции с красивым дефолтным русским текстом на случай отсутствия словарей.
  - Синтаксическая валидация:
    - Проведен успешный запуск `php -l` на всех затронутых PHP файлах. Синтаксических ошибок не обнаружено.

2026-05-24 (Спринт 3: Исправление перекрытия и зависания фронтенда):
ВЫПОЛНЕНО:
  - Устранено зависание/недоступность функционала на страницах Документов, Логов и Настроек:
    - Изменена логика запуска Javascript на страницах `views/admin/documents.php`, `views/admin/logs.php` и `views/admin/settings.php`. Вместо жесткой привязки к `DOMContentLoaded` (который мог не срабатывать, если событие уже наступило на момент разбора шаблона PHP), внедрен надежный readyState-чек. Теперь функции `loadDocs()`, `loadLogs()` и `loadSettings()` гарантированно запускаются немедленно, если DOM уже интерактивен/готов, сохраняя совместимость с отложенным запуском.
  - Исправлен потенциальный критический сбой JS (TypeError) в `public/assets/js/notifications.js`:
    - Добавлены проверки `typeof Notification !== 'undefined'` перед вызовами API уведомлений. Это полностью защищает скрипт от падения в браузерах без поддержки Notifications API (например, на локальном хосте HTTP или в некоторых мобильных браузерах) и исключает блокировку выполнения остального JS-кода.
  - Локализация страницы Документов:
    - Выявлено полное отсутствие локализационных ключей `admin_docs_*` в словарях.
    - В языковые файлы `lang/ru.json` и `lang/kk.json` добавлено 30+ недостающих переводов для всех кнопок, статусов, таблиц и фильтров страницы управления документами.
  - Синтаксическая валидация:
    - Проведен успешный запуск `php -l` на всех измененных шаблонах. Синтаксических ошибок не обнаружено.

2026-05-24 (Спринт 3: Полная ликвидация ReferenceError и стандартизация AJAX в админке):
ВЫПОЛНЕНО:
  - Исправлен критический сбой Javascript (ReferenceError: BASE_URL is not defined) в `views/admin/settings.php` (строки 342, 364):
    - Все обращения к глобальной переменной `BASE_URL` внутри JS-методов `autoSave` и `loadSettings` заменены на стандартное свойство `window.BASE_URL`, инициализируемое в глобальной шапке layout. Это полностью восстановило работоспособность автосохранения и загрузки настроек, разблокировав интерфейс настроек.
  - Стандартизированы fetch-запросы в `views/admin/logs.php` (строки 382, 466):
    - PHP-инъекция `'<?= BASE_URL ?>'` в JS-коде загрузки и очистки логов заменена на стандартизированный JS-шаблон `${window.BASE_URL}`. Это гарантирует 100% совместимость при развертывании как в поддиректориях (XAMPP), так и на корневых доменах (продуктив), полностью исключая 404 ошибки на AJAX-запросах.
  - Успешная валидация:
    - Измененные файлы успешно проверены через `php -l`. Никаких синтаксических ошибок не обнаружено.

2026-05-24 (Спринт 3: Внедрение Cache Busting для предотвращения кэширования старых скриптов):
ВЫПОЛНЕНО:
  - Внедрен механизм Cache Busting (сброс кэша браузера через добавление динамического параметра версии `?v=<?= time() ?>`) для всех критических активов в глобальном макете `views/layouts/admin.php`:
    - `assets/css/dashboard.css`
    - `assets/js/csrf.js`
    - `assets/js/analytics.js`
    - `assets/js/notifications.js`
  - Это принудительно заставляет браузеры клиентов (и прокси/CDN на хостинге) загрузить свежие исправленные скрипты (включая критический фикс Notifications API и BASE_URL в settings/logs), полностью устраняя проблему, когда из-за старого кэша `notifications.js` крашил JS-поток на страницах Настроек, Документов и Логов.
  - Успешная валидация:
    - Файл макета `admin.php` успешно проверен через `php -l` на синтаксическую корректность.





